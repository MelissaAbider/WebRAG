/**
 * Initialise la zone de texte pour le commentaire de l'utilisateur
 */
function initCommentRegion(){
    const commentRegion = document.getElementById('comment');
    commentRegion.value = '';
}