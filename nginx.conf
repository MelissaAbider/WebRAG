server {
    listen 80;

    location / {
        root /usr/share/nginx/html;  # Chemin vers votre front-end
        try_files $uri $uri/ /index.html;  # Redirige vers index.html pour les SPA
    }
}
